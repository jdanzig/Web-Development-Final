body {
  background-image: url("<%= asset_path 'background.png' %>");
  background-repeat: both;
  background-opacity: 0.5;
}

div.pagination {
  display: block;
  clear: both;
  margin: 0 auto;
  text-align: center;
  ul.pagination {
    text-align: center;
  }
}