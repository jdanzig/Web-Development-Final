<%= simple_form_for @ingredient do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :picture %>
  <%= f.button :submit, 'Submit' %> <%= link_to "Cancel", :back, {:class=>"btn btn-default"} %>
<% end %>