
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Final</title>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <nav class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <%= navbar_entry 'Home', root_path %>
            <%= navbar_entry 'Path1', root_path %>
            <%= navbar_entry 'Path2', root_path %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <%= navbar_entry 'Log out', logout_path, :method => :delete if logged_in? %>
            <%= navbar_entry 'Sign up', new_user_path, :method => :get if logged_out? %>
            <%= navbar_entry 'Log in', login_path, :method => :get if logged_out? %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>