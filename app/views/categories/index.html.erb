<h1>Meal Categories</h1>
<p>
You are <%= 'not' unless logged_in? %> logged in.
<% if logged_in? %>
  Hi <%= current_user.first_name %> <%= current_user.last_name %>!
<% end %>
</p>

<p>
<% if logged_in? %>
  <%= button_to 'Log out', logout_path, :method => :delete %>
<% else %>
  <%= button_to 'Log in', login_path, :method => :get %>
<% end %>
</p>